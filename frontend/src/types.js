// Type definitions as JSDoc comments for reference

/**
 * @typedef {'map' | 'modes' | 'report' | 'live' | 'profile'} Screen
 */

/**
 * @typedef {Object} RouteOption
 * @property {string} id
 * @property {string} name
 * @property {string} destination
 * @property {number} time
 * @property {number} accessibility
 * @property {string} safety
 * @property {string} distance
 * @property {'recommended' | 'fastest' | 'direct'} type
 */

export {};
